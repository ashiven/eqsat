// UNSUPPORTED: always
// RUN: %mim %s -o -
plugin compile;

// There are some phases that substitute lambda var uses with the
// lambda bodies (beta reduction?) which we skip with this pipeline.
lam extern _compile(): %compile.Phase =
    %compile.phases ff (
        %compile.named_phase "%eqsat.eqsat_phase",
    );

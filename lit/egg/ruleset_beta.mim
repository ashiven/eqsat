// RUN: MIM_PLUGIN_PATH=%S/.. %mim %s -o -
plugin core;
plugin eqsat;
import _pipeline;

lam extern _config() =
    %eqsat.config (
        %eqsat.egg,
        %eqsat.AstSize,
        %eqsat.rulesets (%eqsat.beta),
    );

